{
	"name": "lab",
	"version": "1.0.0",
	"description": "A bunch of experiments, etc",
	"main": "index.js",
	"repository": "git@github.com:McInkay/bingo.git",
	"author": "Al McKinlay <github@10people.co.uk>",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"build": "npm run lint:css && make build",
		"dev": "npm run lint:css && make build && cd build && npx http-server -p 8080 -c-1",
		"homepage:dev": "cd homepage && npx http-server -p 8080 -c-1",
		"telegram:dev": "cd telegram-bot && node --env-file=.env index.js",
		"telegram:install": "cd telegram-bot && npm install",
		"lint:css": "stylelint '**/*.css' --ignore-path .gitignore"
	},
	"devDependencies": {
		"stylelint": "^16.0.0",
		"stylelint-config-standard": "^36.0.0"
	}
}
