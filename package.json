{
	"name": "lab",
	"version": "1.0.0",
	"description": "A bunch of experiments, etc",
	"main": "index.js",
	"repository": "git@github.com:McInkay/bingo.git",
	"author": "Al McKinlay <github@10people.co.uk>",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"build": "npm run lint:css && make build",
		"dev": "npm run build && netlify dev",
		"bingo:dev": "cd bingo && netlify dev",
		"game-draft:dev": "cd game-draft && netlify dev",
		"homepage:dev": "cd homepage && netlify dev",
		"pokemon-list:dev": "cd pokemon-list && netlify dev",
		"telegram:dev": "cd telegram-bot && node --env-file=.env index.js",
		"switch:dev": "cd switch && netlify dev",
		"lint:css": "stylelint '**/*.css' --ignore-path .gitignore",
		"prepare": "husky"
	},
	"lint-staged": {
		"**/*.css": "stylelint --ignore-path .gitignore"
	},
	"devDependencies": {
		"husky": "^9.1.7",
		"lint-staged": "^16.2.7",
		"stylelint": "^16.0.0",
		"stylelint-config-standard": "^40.0.0"
	}
}
